<!-- HERO SECTION -->
<section class="container" style="margin-top: 6rem; max-width: 1100px;">

  <div style="
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 3rem;
 

  ">

    <!-- LEFT SIDE: Branding + Headline -->
    <div>

      <!-- Logo Block -->
      <div style="
        width: 90px;
        height: 90px;
        border-radius: 24px;
        background-color: #e8f0fe;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
      ">
        <span style="font-size: 44px; color: #1a73e8;">📁</span>
      </div>

      <!-- Product Name -->
      <h2 style="color: #1a73e8; font-weight: 600; margin-bottom: 0.5rem; letter-spacing: 0.3px;">
        File Vault
      </h2>

      <!-- Headline -->
      <h1 style="
        font-weight: 700;
        margin-bottom: 1rem;
        color: #202124;
        font-size: 2.4rem;
        line-height: 1.25;
      ">
        Stop Hunting for Files. Find Every Document in Seconds.
      </h1>

      <!-- Subheader -->
      <p style="
        color: #5f6368;
        font-size: 1.2rem;
        max-width: 500px;
        line-height: 1.6;
      ">
        Access your documents from any device, anywhere — securely stored and organized.
      </p>

    </div>

    <!-- RIGHT SIDE: CTA -->
    <!-- RIGHT SIDE: CTA -->
    <div style="text-align: right;">

      <% if user_signed_in? %>

        <%= link_to "Go to Your Vault", folders_path,
      class: "contrast outline",
      style: "
        padding: 0.85rem 1.6rem;
        font-size: 1.05rem;
        border-radius: 8px;
        display: inline-block;
      " %>

      <% else %>

        <!-- Main CTA -->
        <%= link_to "Start Organizing Today, Create Your Free Account",
        new_user_registration_path,
        class: 'contrast outline',
        style: '
          padding: 0.85rem 1.6rem;
          font-size: 1.05rem;
          border-radius: 8px;
          display: inline-block;
          margin-bottom: 0.8rem;
          text-align: right;
        ' %>

        <!-- Secondary CTA -->
        <p style="color: #5f6368; font-size: 0.95rem; margin: 0;">
          Already have an account?
          <%= link_to "Sign In", new_user_session_path, style: "color: #1a73e8; font-weight: 500;" %>
        </p>

      <% end %>

    </div>

  </div>

</section>
