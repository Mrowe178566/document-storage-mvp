<nav class="navbar navbar-expand-lg navbar-light bg-light" role="navigation">
  <div class="container d-flex justify-content-between align-items-center">

    <!-- Brand -->
    <%= link_to "File Vault", root_path, class: "navbar-brand fw-semibold" %>

    <% if user_signed_in? %>
      <!-- Left side -->
      <div>
        <%= link_to "Dashboard", folders_path, class: "btn btn-outline-primary btn-sm" %>
      </div>
      <!-- Right side -->
      <div>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete,
            class: "btn btn-outline-secondary btn-sm" %>
      </div>
    <% else %>
      <!-- Right side when logged out -->
      <div class="ms-auto d-flex gap-2">
        <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-primary btn-sm" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-sm" %>
      </div>
    <% end %>

  </div>
</nav>
